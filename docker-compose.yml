services:
  api:
    build: .
    container_name: inventario-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./inventario.db:/app/inventario.db
    restart: unless-stopped
